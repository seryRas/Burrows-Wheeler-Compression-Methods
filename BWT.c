#include "BWT.h"

unsigned short sortTable(unsigned char *input, int *indexes) {
    sortingArray arr[0xFF] = {0};
    
}

unsigned short bwtTransform(unsigned char* input, bwt_out* output) {
    int *indexes = malloc(sizeof(size_t) * output->size);
    return 0;
}